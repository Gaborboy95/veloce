name: veloce_lua_core
description: Pure Dart foundations for a sandboxed, dynamically loadable Veloce Lua plugin runtime.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  crypto: ^3.0.6
  cryptography: ^2.9.0
  sqlite3: ^3.5.2

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
